json 파일들 test파일들 만듦
그 파일들을 (테스트용 8개의 json 파일들)
json sample 파일들이 있음
요놈들을 읽어서 어휘분석을 lex를 이용해서 해야함
여기서 사용되는 토큰들 (왼쪽 중괄호, 스트링, :이라는 토큰, 숫자, ","라는 토큰 등등
각 토큰을 인식하는 토큰을 만들어준다.
근데 그 렉스 입력파일은 오픈을 해주었음
json.l
#으로 시작하는건 comment (지워도 상관 x)
적용되는 규칙이 여러개가있으면 가장우선순위 높은건 긴거, 그다음 순서
name만 인식하고싶었는데 우선순위 높은걸로하다보니
"name": "Foo 이게 인식되버림 (파서 에러)
^a 는 a를 제외하고
[0-9] -> 0~9
[^0-9] -> 0~9 제외하고
[^\"]* -> "를 제외하고
해결방법
\"[^\"]*\"	{ strcpy(yylval.str, yytext); return(STRING); }
이렇게함
암튼 렉스파일은만들어져있음
이제 야크 입력파일을 만들어야함
josn-cfg-Good 보삼
lex에서 인식한놈들은 빼버림
CFG까지는 기술해져있고
action part를 만들면 됨 
ex) { puts(" members -> pair"); }
어떻게 완성시켜야함?
파스트리를 구성해라 순서대로 모아서 한꺼번에 파스트리를 만들어도되고
만들면서 서브트리를 만들어도되고 편할대로
test1에 대해서 했다면
파스트리는 예를들자면
		E
	E	*	E
     E	+  E		a
     a     a
이렇게있다하면 90도를 왼쪽으로 돌려서 출력하면 쉬움

그다음 3번하면됨
